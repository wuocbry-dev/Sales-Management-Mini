spring.application.name=sales_management_mini
spring.profiles.active=dev

<<<<<<< Updated upstream
# # --- MySQL trên máy bạn: dùng biến môi trường (an toàn) hoặc ghi trực tiếp trong application-dev.properties ---
# # Ví dụ PowerShell: $env:DB_URL="jdbc:mysql://127.0.0.1:3306/quanlybanhang?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh"
# #                   $env:DB_USERNAME="root"; $env:DB_PASSWORD="matkhau"
# spring.datasource.url=${DB_URL:jdbc:mysql://100.78.133:1776/sales_management_mini?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh}
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=false
# spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# # JWT (HS256, secret băm SHA-256 trong JwtService)
# app.jwt.secret=${JWT_SECRET:dev-only-change-in-production-minimum-32-characters-long}
# app.jwt.expiration-ms=86400000

# Đăng ký công khai: luôn gán role mặc định STORE_MANAGER (quản lý cửa hàng). SYSTEM_ADMIN chỉ tạo qua seed SQL / tài khoản quản trị.
app.auth.register-default-role=${REGISTER_DEFAULT_ROLE:STORE_MANAGER}
spring.datasource.url=jdbc:mysql://localhost:3306/sales_management_mini?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Đồng bộ cột bảng với JPA (ví dụ branches.phone / branches.email). Production: đặt JPA_DDL_AUTO=validate hoặc none và chạy script trong Docx/sql/migrations/.
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# Upload ảnh sản phẩm
app.upload.product-images-dir=${PRODUCT_IMAGES_DIR:uploads/products}
=======
# Cấu hình Upload File chung
>>>>>>> Stashed changes
spring.servlet.multipart.max-file-size=${MAX_UPLOAD_FILE_SIZE:3MB}
spring.servlet.multipart.max-request-size=${MAX_UPLOAD_REQUEST_SIZE:12MB}

# App Custom Config chung
app.auth.register-default-role=${REGISTER_DEFAULT_ROLE:STORE_MANAGER}
app.upload.product-images-dir=${PRODUCT_IMAGES_DIR:uploads/products}

# JPA chung
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh
spring.jpa.properties.hibernate.format_sql=true

# JWT chung (Có thể override ở prod)
app.jwt.secret=${JWT_SECRET:dev-only-change-in-production-minimum-32-characters-long}
app.jwt.expiration-ms=86400000

# Flyway mặc định tắt
spring.flyway.enabled=false