# Kích hoạt: SPRING_PROFILES_ACTIVE=docker (docker-compose mặc định đã bật).
# CORS rộng cho HTTP — chỉ dùng khi chạy stack Docker nội bộ; production nên thu hẹp origin.
app.cors.allow-all-http-origins=true
